version: "3.9"
services:
  mini-rproxy:
    build: .
    image: mini-rproxy:latest
    environment:
      - LOG_MODE=json
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: "1g"